.DS_Store
.env
.env.*
*.log

node_modules/
.next/
dist/
out/

# Regenerated on every `tsc` run, and committing it means every typecheck shows
# up as a diff.
tsconfig.tsbuildinfo

joi-doorway-video/node_modules/
joi-doorway-video/out/
joi-doorway-video/dist/
.env.local

# Tool scratch directories — skill working files and session plans, not project source.
.img2threejs/
.zcode/
.impeccable/

# Locally installed user-requested skill store package; not website source.
/skills/

# Blender working sources and QA renders. AGENTS.md keeps assets/3d/ out of the
# repo on purpose; these rules stop 145 MB of it showing up as untracked noise.
# Files already tracked here (about-room.blend, about-room-bakes/) are unaffected.
/assets/3d/
__pycache__/
